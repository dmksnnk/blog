# Elasticsearch Integration Test Example

This example demonstrates how to run integration tests against Elasticsearch running in container.

Run the test:

```sh
make run
```
This will start Elasticsearch in Docker and run the tests.

Cleaning up:

```sh
make down
```
